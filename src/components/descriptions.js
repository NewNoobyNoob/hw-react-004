//mounting methods
//updating methods
//unmounting methods
//error handling

//mounting includes 4 (four) lifecycle methods: 1. constructor(props); 2. static getDerivedStateFromProps(props, state); 3. render(); 4. componentDidMount().

//updating includes 5 (five) lifecycle methods: 1. static getDerivedStateFromProps(props, state); 2. shouldComponentUpdate(nextProps, nextState); 3. render(); 4. getSnapshotBeforeUpdate(prevProps, prevState); and 5. componentDidUpdate(prevProps, prevState, snapshot).

//unmounting includes 1 (one) lifecycle method: 1. componentWillUnmount().

//error handling includes 2 (two) lifecycle methods: 1. static getDerivedStateFromError(error); and 2. componentDidCatch(error, info).

